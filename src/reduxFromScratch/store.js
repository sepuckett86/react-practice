const state = {};

function getState() {
  return state;
}

function dispatch(action) {
  
}

// {
  type: x
  payload: y
}


const store = { getState, dispatch, subscribe, replaceReducer };
export default store;
